{
  "name": "attache",
  "version": "0.1.1",
  "private": true,
  "description": "Web GUI for spinning up and managing containerized AI agents",
  "type": "module",
  "workspaces": [
    "server",
    "web"
  ],
  "scripts": {
    "dev": "concurrently -n server,web -c blue,green \"npm run dev --workspace server\" \"npm run dev --workspace web\"",
    "build": "npm run build --workspace web",
    "start": "npm run start --workspace server",
    "create-admin": "npm run create-admin --workspace server --"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
